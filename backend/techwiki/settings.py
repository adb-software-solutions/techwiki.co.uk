"""
Django settings for techwiki project.

Generated by 'django-admin startproject' using Django 4.2.4.

For more information please see
https://docs.djangoproject.com/en/4.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.2/ref/settings/
"""

import base64
import binascii
import logging
import os
import sys
from pathlib import Path

import django_stubs_ext
from django.templatetags.static import static

logger = logging.getLogger(__name__)

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = bool(int(os.environ.get("DEBUG", "0")))
DEBUG_TOOLBAR_ENABLED = bool(int(os.environ.get("DEBUG_TOOLBAR_ENABLED", "0")))

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
RUNNING_MYPY = Path(sys.argv[0]).name == "mypy"
if DEBUG or RUNNING_MYPY:
    SECRET_KEY = os.environ.get("SECRET_KEY", "django-insecure-techwiki-development-only")
else:
    SECRET_KEY = os.environ["SECRET_KEY"]

if DEBUG:
    # will output to your console
    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s %(levelname)s %(message)s",
    )

SITE_DOMAIN = os.environ.get("SITE_DOMAIN", "localhost")

FRONTEND_URL = os.environ.get("FRONTEND_URL", "http://localhost:5173")
AUTH_FRONTEND_URL = os.environ.get("AUTH_FRONTEND_URL", "http://localhost:5175")

MICROSOFT_GRAPH_TENANT_ID = os.environ.get("MICROSOFT_GRAPH_TENANT_ID", "")
MICROSOFT_GRAPH_CLIENT_ID = os.environ.get("MICROSOFT_GRAPH_CLIENT_ID", "")
MICROSOFT_GRAPH_SENDER_EMAIL = os.environ.get("MICROSOFT_GRAPH_SENDER_EMAIL", "")
MICROSOFT_GRAPH_CERTIFICATE_BASE64 = os.environ.get("MICROSOFT_GRAPH_CERTIFICATE_BASE64", "")
MICROSOFT_GRAPH_PRIVATE_KEY_BASE64 = os.environ.get("MICROSOFT_GRAPH_PRIVATE_KEY_BASE64", "")
MICROSOFT_GRAPH_CERTIFICATE_PATH = os.environ.get("MICROSOFT_GRAPH_CERTIFICATE_PATH", "")
MICROSOFT_GRAPH_PRIVATE_KEY_PATH = os.environ.get("MICROSOFT_GRAPH_PRIVATE_KEY_PATH", "")
MICROSOFT_GRAPH_PRIVATE_KEY_PASSPHRASE = os.environ.get(
    "MICROSOFT_GRAPH_PRIVATE_KEY_PASSPHRASE", ""
)
MICROSOFT_GRAPH_TIMEOUT_SECONDS = int(os.environ.get("MICROSOFT_GRAPH_TIMEOUT_SECONDS", "15"))

if not SITE_DOMAIN:
    raise ValueError("SITE_DOMAIN must be set.")

if DEBUG:
    ALLOWED_HOSTS: list[str] = ["*"]
else:
    ALLOWED_HOSTS = [
        "api." + SITE_DOMAIN,
    ]


# Application definition

INSTALLED_APPS = [
    "apps.wiki",
    "apps.analytics",
    "authentication",
    "corsheaders",
    "unfold",  # before django.contrib.admin
    "unfold.contrib.filters",  # optional, if special filters are needed
    "unfold.contrib.forms",  # optional, if special form elements are needed
    "unfold.contrib.inlines",  # optional, if special inline elements are needed
    "django.contrib.admin",
    "django.contrib.auth",
    "django.contrib.contenttypes",
    "django.contrib.sessions",
    "django.contrib.messages",
    "django.contrib.staticfiles",
]

UNFOLD = {
    "SITE_TITLE": "Tech Wiki Admin",
    "SITE_HEADER": "Tech Wiki Admin",
    "SITE_ICON": {
        "light": lambda request: static("logo.png"),
        "dark": lambda request: static("logo-dark.png"),
    },
    "SITE_LOGO": {
        "light": lambda request: static("logo.png"),
        "dark": lambda request: static("logo-dark.png"),
    },
}

if DEBUG_TOOLBAR_ENABLED:
    INSTALLED_APPS += ["debug_toolbar"]

AUTH_USER_MODEL = "authentication.User"

MIDDLEWARE = [
    "corsheaders.middleware.CorsMiddleware",
    "django.middleware.security.SecurityMiddleware",
    "django.contrib.sessions.middleware.SessionMiddleware",
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.contrib.auth.middleware.AuthenticationMiddleware",
    "django.contrib.messages.middleware.MessageMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
    "ninja.compatibility.files.fix_request_files_middleware",
    "apps.wiki.middleware.SanitizeRenderedHtmlMiddleware",
]

if DEBUG_TOOLBAR_ENABLED:
    MIDDLEWARE += ["debug_toolbar.middleware.DebugToolbarMiddleware"]

AUTHENTICATION_BACKENDS = [
    "authentication.backends.EmailBackend",
]

ROOT_URLCONF = "techwiki.urls"

TEMPLATES = [
    {
        "BACKEND": "django.template.backends.django.DjangoTemplates",
        "DIRS": [os.path.join(BASE_DIR, "templates")],
        "APP_DIRS": True,
        "OPTIONS": {
            "context_processors": [
                "django.template.context_processors.debug",
                "django.template.context_processors.request",
                "django.contrib.auth.context_processors.auth",
                "django.contrib.messages.context_processors.messages",
            ],
            "debug": DEBUG,
        },
    },
]

ASGI_APPLICATION = "techwiki.asgi.application"

# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

DATABASES = {
    "default": {
        "ENGINE": os.environ.get("SQL_ENGINE", "django.db.backends.postgresql"),
        "NAME": os.environ.get("SQL_DATABASE", "techwiki_dev"),
        "USER": os.environ.get("SQL_USER", "techwiki"),
        "PASSWORD": os.environ.get("SQL_PASSWORD", "techwiki_dev_password"),
        "HOST": os.environ.get("SQL_HOST", "db"),
        "PORT": os.environ.get("SQL_PORT", "5432"),
        "OPTIONS": {
            "sslmode": os.environ.get("SQL_SSL", "disable"),
        },
    }
}


# We need to check if the below settings are base64 encoded and if so, decode them. If already decoded, we can use them as is.
def decode_base64(value: str) -> str:
    try:
        decoded_value = base64.b64decode(value).decode("utf-8")
        return decoded_value
    except (binascii.Error, UnicodeDecodeError):
        return value


# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        "NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.MinimumLengthValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.CommonPasswordValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.NumericPasswordValidator",
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = "en-us"

TIME_ZONE = "UTC"

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = "static/"

STATICFILES_DIRS = [
    os.path.join(BASE_DIR, "static"),
]

STATIC_ROOT = os.path.join(BASE_DIR, "staticfiles")

# Default primary key field type
# https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"

MEDIA_URL = "/media/"
MEDIA_ROOT = os.path.join(BASE_DIR, "media/")

django_stubs_ext.monkeypatch()

SITE_ID = 1
SITE_NAME = "Tech Wiki"

X_FRAME_OPTIONS = "SAMEORIGIN"

CSRF_COOKIE_HTTPONLY = False
CSRF_COOKIE_SAMESITE = "Lax"
CSRF_COOKIE_SECURE = True
CSRF_COOKIE_DOMAIN = "." + SITE_DOMAIN if not DEBUG else None

CORS_ALLOWED_ORIGINS = [
    "https://" + SITE_DOMAIN,
    "https://api." + SITE_DOMAIN,
    "https://auth." + SITE_DOMAIN,
]

if DEBUG:
    CORS_ALLOWED_ORIGINS += [
        "http://localhost:3000",
        "http://localhost:5173",
        "http://localhost:5175",
    ]

CSRF_TRUSTED_ORIGINS = CORS_ALLOWED_ORIGINS

SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")

CORS_ALLOW_HEADERS = [
    "accept",
    "accept-encoding",
    "authorization",
    "content-type",
    "dnt",
    "origin",
    "user-agent",
    "X-CSRFToken",
    "sentry-trace",
    "baggage",
]

CORS_ALLOW_CREDENTIALS = True

SESSION_ENGINE = "django.contrib.sessions.backends.cache"
SESSION_COOKIE_SAMESITE = "Lax"
SESSION_COOKIE_SECURE = True
SESSION_COOKIE_AGE = 864000
SESSION_COOKIE_DOMAIN = "." + SITE_DOMAIN

SESSION_CACHE_ALIAS = "session"

DATA_UPLOAD_MAX_MEMORY_SIZE = 500 * 1024 * 1024
FILE_UPLOAD_MAX_MEMORY_SIZE = 500 * 1024 * 1024

CACHES = {
    "default": {
        "BACKEND": "django_redis.cache.RedisCache",
        "LOCATION": os.environ.get("CACHE_BACKEND_URL", ""),
        "OPTIONS": {
            "CLIENT_CLASS": "django_redis.client.DefaultClient",
        },
    },
    "session": {
        "BACKEND": "django_redis.cache.RedisCache",
        "LOCATION": os.environ.get("SESSION_BACKEND_URL", ""),
        "OPTIONS": {
            "CLIENT_CLASS": "django_redis.client.DefaultClient",
        },
    },
}

if DEBUG:
    SESSION_COOKIE_SAMESITE = "None"
    SESSION_COOKIE_SECURE = True
    # Disable rate limiting in development since Redis might not be available
    RATE_LIMIT_DISABLED = True
